//This thing broke my mind for a while
//Work in this when you can
/*//Scroll only if the hp is different than the numbers shown
showH = showH != real(hundreds) * 8 ? showH + 1: real(hundreds) * 8;
showT = showT != real(tens) * 8 ? showT + 1: real(tens) * 8;
showO = showO != real(ones) * 8 ? showO + 1: real(ones) * 8;

if string_length(global.hp[i]) < 3
{
	showH = (real(showH) < 100 && showH > 10) ? "0" + string(showH): showH;
	showT = (real(showT) < 100 && showT > 10) ? "0" + string(showT): showT;
	showO = (real(showO) < 100 && showO > 10) ? "0" + string(showO): showO;
	
	showH = (real(showH) < 10) ? "0" + "0" + string(showH): showH;
	showT = (real(showT) < 10) ? "0" + "0" + string(showT): showT;
	showO = (real(showO) < 10) ? "0" + "0" + string(showO): showO;
}

//Numbers back to 0 after the last frame
showH = (showH >= 79) ? 0: showH;
showT = (showT >= 79) ? 0: showT;
showO = (showO >= 79) ? 0: showO;*/